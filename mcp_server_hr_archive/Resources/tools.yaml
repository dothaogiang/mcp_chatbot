tools:
  - category: mcp
    name_tool: search_archive
    module: tools.definitions.search_archive
    attr: search_archive
    enabled: true
    description: "Tìm kiếm hồ sơ lưu trữ theo từ khóa và bộ lọc"

  - category: mcp
    name_tool: get_archive_detail
    module: tools.definitions.get_archive_detail
    attr: get_archive_detail
    enabled: true
    description: "Lấy chi tiết đầy đủ 1 hồ sơ lưu trữ theo UUID"

  - category: mcp
    name_tool: search_staff_profile
    module: tools.definitions.search_staff_profile
    attr: search_staff_profile
    enabled: true
    description: "Tìm hồ sơ cán bộ bằng RAG/hybrid search"

  - category: mcp
    name_tool: get_archive_file_info
    module: tools.definitions.get_archive_file
    attr: get_archive_file_info
    enabled: true
    description: "Trả về metadata của file hồ sơ (KHÔNG trả binary)"
